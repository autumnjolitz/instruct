def __getitem__(self, item):
    '''
    Autogenerated code
    '''
    {%- if properties %}
    if item in {{frozenset(properties)}}:
        {%- for field in frozenset(properties) %}
        {%if not loop.first%}el{%endif%}if item == '{{field}}':
            return self.{{field}}
        {%- endfor %}
    {%- endif %}
    return self._data[item]

def __setitem__(self, key: str, val) -> None:
    '''
    Autogenerated code
    '''
    {%- for field in frozenset(chain(fields, properties)) %}
    {%if not loop.first%}el{%endif%}if key == '{{field}}':
        self.{{field}} = val
        return
    {%- endfor %}
    raise KeyError(key)
