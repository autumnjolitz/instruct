with define_data_class():
    class _{{class_name}}(klass):
        __slots__ = {{slots}}
        # Copy any custom __eq__, etc into the data class
        # so that we can allow the user to call super()
        # to defer to the autogenerated __eq__, etc
        {% for key in data_class_attrs %}
        {{key}} = _dataclass_attrs["{{key}}"]
        {% endfor %}
